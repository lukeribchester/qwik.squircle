import { component$, useStyles$ } from "@builder.io/qwik";
import styles from "./reveal.css?inline";

export const QwikSquircle = component$(() => {
  useStyles$(styles);

  return (
    <></>
  );
});
